source 'https://rubygems.org'

gem 'sqlite3'  # database for storing Dash docset
gem 'nokogiri' # HTML parser
gem 'plist'    # docsets use plist files as per Mac software convention
gem 'thor'     # CLI interface for tasks/generators
